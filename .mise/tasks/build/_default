#!/usr/bin/env bash

#MISE description="Build the Dainty application (Release)"
#MISE sources=["Cargo.toml", "sqlc.yaml", "src/**/*.rs", "assets/**", "queries"]
#MISE outputs=["target/release/dainty", "target/frontend/**"]
#MISE depends=["test", "generate:sql"]

echo "🚀 Starting Dainty release build..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

cargo build --release
