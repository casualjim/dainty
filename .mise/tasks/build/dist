#!/usr/bin/env bash

#MISE description="Build the distribution of the Dainty application (Release)"
#MISE sources=["target/release/dainty", "target/frontend/**"]
#MISE outputs=["dist/**"]
#MISE depends=["test", "build"]

echo "🚀 Starting Dainty release build..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

mkdir -p dist
cp -r target/frontend dist/public
cp target/release/dainty dist/dainty
