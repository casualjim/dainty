#!/usr/bin/env bash

#MISE description="Build the Dainty docker image"
#MISE sources=["dist/**"]
#MISE depends=["test", "build", "build:dist"]

echo "🚀 Starting Dainty release build..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

docker build --pull -t ghcr.io/casualjim/dainty:latest -f Dockerfile .
